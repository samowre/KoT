delta: THEORY
BEGIN
  δ(Γ)
    CASES U OF
      v(x):
        IF definition?(Γ)(x)
	  THEN δ(rest(Γ, x))(definition(Γ)(x))
	  ELSE v(x)
	ENDIF
    , dot(m, S, x):
    , interp(m, σ):
        interp(δ(Γ)(m), σ)
    , fun(A, B):
    , prod
    , subtype
END delta
