delta: THEORY
BEGIN
  δ(Γ)
    CASES U OF
      v(x):
        IF definition?(Γ)(x)
	  THEN δ(rest(Γ, x))(definition(Γ)(x))
	  ELSE v(x)
	ENDIF
    , dot(m, S, x):
        LET Θ = δ(Γ)(m) IN
	η(signature(Γ))(signature(Θ), 
    , interp(m, σ):
        Γ(m)
    , fun(A, B):
    , prod
    , subtype
END delta
